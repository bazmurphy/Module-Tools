#!/bin/bash

set -euo pipefail

# TODO: Write a command to output the contents of the helper-1.txt file inside the helper-files directory to the terminal.
# The output of this command should be "Once upon a time...".

cat ../helper-files/helper-1.txt

# feedback:
# do they know how to navigate from the root of this project / to the correct place to start?
# do they know they need to run ./script-name.sh from the terminal? (and why they need to do it this way)
# do they know about ../ by this point (re above answer)?
# these are all things that may cause friction (intended or not?)
